<h1>Lista de Usuarios</h1>
<div class="panel panel-default">
  
  <div class="panel-heading">
  
  </div>

  <table class="table table-striped table-bordered  ">
    <thead>
      <tr>
        <th>Nombre</th>
        <th>Apellidos</th>
        <th>Telefono</th>
        <th>Celular</th>
        <th>Direccion</th>
        <th>Email</th>
  <!--  <th>Crypted password</th> 
        <th>Salt</th>    -->

        <th>Funciones</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= user.nombre %></td>
          <td><%= user.apellido %></td>
          <td><%= user.telefono %></td>
          <td><%= user.celular %></td>
          <th><%= user.dir %></th>
          <td><%= user.email %></td>
  <!--    <td><%= user.crypted_password %></td>   
          <td><%= user.salt %></td>  -->
          <td><%= link_to 'Mostrar', user %>
              
              <%= link_to t('.edit', :default => t("helpers.links.Editar")),
                        edit_user_path(user), :class => 'btn btn-mini' %>
              <%= link_to 'Eliminar', user, method: :delete, data: { confirm: 'Esta seguro?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
<br>

<%= link_to 'Nuevo Usuario', new_user_path %>
