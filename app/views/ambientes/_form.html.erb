<%= simple_form_for @ambiente, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :nombre %>
  <%= f.input :ubicacion %>
  <%= f.input :codigo %>
  <%= f.input :descripcion %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                ambientes_path, :class => 'btn' %>
  </div>
<% end %>
