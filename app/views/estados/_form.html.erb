<%= simple_form_for @estado, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :nombre %>
  <%= f.input :descripcion %>
  <div class="form-actions">
    <%= f.button :submit, 'Crear Estado', :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.Cancelar")),
                estados_path, :class => 'btn' %>
  </div>
<% end %>
